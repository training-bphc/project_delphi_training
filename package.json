{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" ",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "db:reup": "npm run db:reset --prefix server && npm run db:migrate --prefix server && npm run db:seed --prefix server"
  }
}