Project Delphi - Current Workspace Update Summary
Updated on: 2026-03-24

==================================================================
1) MODIFIED FILES (Tracked files changed)
==================================================================

1. README.md
	- Root setup guide updated to include end-to-end local testing flow.
	- Added clearer backend + frontend startup order and quick test path.

2. client/README.md
	- Added local development login guidance.
	- Documented client env usage and API proxy target behavior.

3. client/src/contexts/auth.tsx
	- Added development login method (devLogin).
	- Improved error handling for non-JSON or empty backend responses.
	- Added user-facing fallback error when backend is unavailable.

4. client/src/pages/Login.tsx
	- Added development quick-login buttons (student/admin).
	- Integrated CSS module based login layout and styling hooks.

5. client/vite.config.ts
	- Proxy target is now environment-driven via VITE_API_PROXY_TARGET.
	- Keeps /api proxy flexible across different backend targets.

6. server/.env.example
	- Updated to DATABASE_URL-first format.
	- Retained DB_HOST/DB_USER style variables as optional fallback.
	- Updated CLIENT_URL recommendation to Vite default (5173).

7. server/README.md
	- Updated environment variable documentation.
	- Added dev-login route note and local testing guidance.

8. server/src/config/db.ts
	- Added DATABASE_URL support with DB_* fallback.
	- Added startup validation for missing DB configuration.
	- Added SSL option for connection-string based deployments.

9. server/src/controllers/authController.ts
	- Added devLogin endpoint (disabled automatically in production).
	- Supports local student/admin login by registered email.

10. server/src/db/migrate.ts
	 - Added migration tracking table (schema_migrations).
	 - Prevents reapplying already applied SQL migration files.

11. server/src/db/migrations/001_initial_schema.sql
	 - Added data integrity checks (year ordering, status check values).
	 - Added training_records -> students foreign key (roll_number + email).

12. server/src/db/seedAll.ts
	 - Seed script updated to include students referenced by test records.
	 - Keeps seeding compatible with stricter foreign key constraints.

13. server/src/index.ts
	 - Improved startup behavior for port conflicts by selecting next free port.
	 - Updated default CLIENT_URL fallback for CORS to localhost:5173.

14. server/src/routes/authRoutes.ts
	 - Added /api/auth/dev-login route.
	 - Route imports cleaned up accordingly.

==================================================================
2) UNTRACKED FILES/FOLDERS (New files not yet committed)
==================================================================

1. BITS Logo.jpg
	- Root-level logo asset present in repository workspace.
    - The Css part of the login.module page is very minimal and very bad, please update it 

2. client/.env.example
	- Client-side template env file.
	- Contains VITE_API_PROXY_TARGET and VITE_GOOGLE_CLIENT_ID placeholders.

3. client/public/BITS Logo.jpg
	- Public asset copy used by login page background styling.

4. client/src/pages/Login.module.css
	- New login page stylesheet.
	- Includes panel layout, translucent logo background, and button styling.

5. instructions.md
	- New documentation file for record constraints and environment transition.

6. server/postman/ (folder)
	- Added Postman assets for local API testing.
	- Includes:
	  - Delphi-Server.postman_collection.json
	  - Delphi-Local.postman_environment.json

7. updates.txt
	- This update summary file.

==================================================================
3) IMPORTANT NOTE
==================================================================

- The client dev server currently depends on the backend API being reachable at
  the configured proxy target (VITE_API_PROXY_TARGET, default localhost:5000).
- If backend is not running, login now shows a clean actionable message
  instead of crashing on JSON parse. It was crashing because the POSTMAN API 
  I used has connecting issue so I manually added a "alert" message 

====================================================================
The updates.txt, instructions.md are generated by GPT, so please take a clear look before 
next proceeding 
====================================================================
